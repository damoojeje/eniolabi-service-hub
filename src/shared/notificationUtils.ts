/**
 * Shared utility functions for notification handling
 * Eliminates duplication across components that need notification icons, colors, and formatting
 */

export type NotificationPriority = 'low' | 'medium' | 'high'

// This file is kept for backward compatibility.
// All notification utilities have been moved to statusConfig.ts
